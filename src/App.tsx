import styled from "styled-components";

const Header = styled.h1`
	margin: 2rem;
	font-size: 4rem;
`;

function App() {
	return (
		<div id="app">
			<Header>It works!</Header>
		</div>
	);
}

export default App;
